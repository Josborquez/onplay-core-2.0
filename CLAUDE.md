# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Qué es este proyecto

`onplay-core` es un sistema POS/ERP para Comercializadora y Distribuidora BM: la **fuente de verdad única** de productos, precios, inventario, clientes y ventas, por encima de dos tiendas WooCommerce existentes (onplay.cl y onplaygames.cl) que **no se reemplazan ni se fusionan**. El documento rector es `docs/01-SDD-general.md` (SDD general v1.2); la spec ejecutable de la Etapa 1 es `docs/02-SDD-etapa1-mostrador.md`.

**Bitácora de revisión:** `docs/08-bitacora-revision.md` registra los hallazgos de la revisión visual con datos reales (numerados `R-NNN`, con estado). Toda novedad, defecto o dato raro del origen que aparezca al usar el sistema se anota ahí ANTES de corregirlo; lo que amplía alcance queda como «Agendado (E#)» y no se construye (P1). R-001/R-002/R-003 (2026-09-02) cambiaron el importador: reglas puras en `apps/api/src/sync/variaciones.ts` (nombre de variación sin repetir el padre, SKU externo con forma de EAN → `codigoBarras`, `atributos.padreExternoId`/`variante`), el hash de sync las incluye y la actualización solo COMPLETA lo que falta (nunca pisa `codigoBarras` ni `atributos` manuales).

**Etapa 2 (inventario) EN CURSO — Fase 0 ratificada por el dueño el 2026-09-02** (regla de prioridad entre canales en 03 §6.9: físico gana; online gana si el pago está realizado). **E2 Fase 1 (cimientos) completada:** migración `20260902195601_e2_inventario` (enums `MotivoStock`/`EstadoRecuento`/`TipoMovimientoCaja`, `AccionAuditoria` + `devolver`/`recuento`/`ajustar_stock`/`vender_reservado` al final; modelos `Ubicacion`, `MovimientoStock`, `StockActual` (@@id productoId+ubicacionId), `Recuento`/`RecuentoLinea`, `Devolucion`/`DevolucionLinea`, `MovimientoCaja`; `Producto.stockMinimo`; `ProductoCanal.stockCanal/manejaStockCanal/stockCanalEn`; back-relations en Producto/Usuario/TurnoCaja/Venta/VentaLinea); semilla idempotente de 4 ubicaciones (`mostrador` esVenta, `bodega` publicable) en `prisma/seed.ts`; dominio `packages/dominio/src/stock.ts` (`validarMovimientoStock`, `firmarCantidadManual`, `aplicarMovimiento`, `estadoStock`, `avisoWeb` §6.9, `cerrarRecuento` §6.4) y `devolucion.ts` (`validarDevolucion` con `prorratearDescuento` por resto mayor — la suma cuadra exacta —, `formatearFolioDevolucion`), `calcularArqueo` gana un 4º parámetro opcional `ExtrasArqueo {devolucionesEfectivo, ingresosCaja, retirosCaja}` sin cambiar E1; 26 tests nuevos (75 en dominio, 103 total). **E2 Fase 2 (libro y venta C1/C2/C3) completada:** `apps/api/src/stock/libro.ts` es el ÚNICO escritor de `StockActual` (`bloquearStock` = INSERT…ON DUPLICATE KEY + SELECT FOR UPDATE; `registrarMovimiento` = validar + INSERT movimiento + UPDATE resumen; `resumenStock`/`adjuntarStock` → `stockTotal/stockVenta/stockCanalMin/estadoStock`; `contextoReserva`; `stockPorUbicacion`; `ErrorStock` con status). Rutas `apps/api/src/rutas/stock.ts` (registrada tras productos): `GET /ubicaciones`, `POST/PATCH /ubicaciones` (esVenta única; desactivar exige stock 0), `GET /stock?ubicacionId&estado&categoriaId&q&pagina` (filtra en memoria, ≤3.4k filas), `GET /productos/:id/stock` (desglose + espejo + `avisoWeb`), `GET /productos/:id/movimientos` (kardex, encargado), `POST /stock/verificar` (admin, solo reporta). `GET /productos`, `buscar` y `catalogo-offline` adjuntan el resumen de stock (caché web esquema 3); `PATCH /productos/:id` acepta `stockMinimo` y apagar `controlaStock` exige `nota` (auditoría `ajustar_stock`). `POST /ventas`: líneas con `controlaStock` agregadas por producto y ORDENADAS por id; regla §6.9 ANTES de la tx (`avisoWeb` con `contextoReserva` → `409 RESERVADO_WEB {productoId, canalId, stockCanal, stockCanalEn, stockPropio, descripcion}`; encargado+ con `forzarReservado:{nota}` sigue y deja Auditoria `vender_reservado`); dentro de la tx: Cliente → `bloquearStock` por producto → Correlativo → crear venta → `registrarMovimiento(-cant, 'venta', ref venta)`; nunca bloquea por stock propio, `advertencias` ganó `tipo` (`PRECIO_DISTINTO` | `STOCK_NEGATIVO {productoId, descripcion, ubicacion, cantidadNueva}`). Anular repone con `devolucion` positiva por cada descuento (original intacto). **Verificado por curl:** venta con stock 0 → 201 + STOCK_NEGATIVO y libro −1; anular → +1; **criterio 5**: dos POST simultáneos por la última unidad → dos 201, exactamente una STOCK_NEGATIVO, `stock_actual = SUM = −1`; **criterio 15b**: vendedor → 409 aunque mande nota, admin con nota → 201 + `vender_reservado`. Web: `EtiquetaStock` compartida en `base.tsx` («stock N» / «sin stock» / «· agotado en la web» / «· último en la web», nunca suma el espejo) en Buscador, AccesoRapido y V5 (`TextoStock` real + `DesgloseStock` por ubicación y espejo en la ficha); `DialogoCobro` maneja `RESERVADO_WEB` con panel «Reservado para un pedido web pagado» (Quitar de la venta / Vender igual solo encargado con motivo → `forzarReservado`), la misma regla offline con `productoPorId` del caché (`reservadosCache`), y muestra «El stock quedó en negativo: …» tras cobrar; `CuerpoVenta` de la cola lleva `forzarReservado`. Verificado en Chrome (panel de bloqueo y venta forzada V-2026-00020 auditada). **E2 Fase 3 (recuentos y movimientos manuales C4/C5) completada:** `POST /stock/movimientos` (encargado; `firmarCantidadManual`: merma resta, compra suma, ajuste con signo; nota obligatoria; ajuste/merma exigen `controlaStock` → 422 SIN_CONTROL_STOCK; `compra` sobre producto sin control lo ENCIENDE con Auditoria `ajustar_stock` — M5), `POST /stock/traslados` (dos `registrarMovimiento` con la misma `referenciaId` = `randomUUID()`, candados por ubicacionId ascendente, exige control), rutas `apps/api/src/rutas/recuentos.ts` (`GET /recuentos` con `porcentajeCuadrado`, `GET /recuentos/:id` con `stockVigente` por línea, `POST /recuentos` por categoría (subárbol, activos, sin `servicio`, tope 500 → 422 RECUENTO_DEMASIADO_GRANDE) o `productoIds`, `POST /recuentos/:id/lineas {productoId, sumar}` = escáner (crea la línea si no estaba), `PATCH /recuentos/:id/lineas/:productoId {cantidadContada|null}`, `POST /recuentos/:id/cerrar` (tx: bloquea contadas ordenadas por productoId → `cerrarRecuento` del dominio contra el stock VIGENTE → movimientos `recuento_inicial`/`ajuste` ref recuento → `controlaStock=true` en contadas → `cantidadSistema` congelada al cierre → Auditoria `recuento`), `POST /recuentos/:id/descartar {nota}`; reabrir/cerrar dos veces → 409 RECUENTO_CERRADO). **Ojo Fastify:** POST con `Content-Type: application/json` y cuerpo vacío → 400; el web manda `body:'{}'` en cerrar y los curl usan `-d '{}'`; cuerpos con tildes desde Git Bash llegan mal codificados (usar ASCII en pruebas). UI: V21 `components/DialogoMovimientoStock.tsx` (Ajuste/Merma/Ingreso/Traslado, ubicación y destino con cantidades, signo automático, nota obligatoria, vista previa «pasará de X a Y», hace el POST), V19 `pantallas/admin/Stock.tsx` (`/admin/stock`: filtros ubicación/categoría/estado/texto, cantidad total o por ubicación, insignia de estado, acciones por fila; «Ingresar y encender» para los sin control), V20 `pantallas/admin/Recuentos.tsx` (`/admin/recuentos` lista con % cuadrado y diálogo Nuevo por ubicación+categoría; `/admin/recuentos/:id` con escáner de foco permanente que suma 1 vía caché local, columnas sistema/contado/diferencia, progreso, «Solo pendientes», cierre con resumen y descarte con nota); barra lateral con Stock ▥ y Recuentos ☑. Verificado por curl (criterios 7, 8, 9; `POST /stock/verificar` sin diferencias) y en Chrome (traslado con vista previa, lista de recuentos, escaneo suma 1). **E2 Fase 4 (alertas, espejo del canal, kardex, CSV) completada:** `packages/woo-client` tipos ganan `manage_stock`/`stock_quantity`/`stock_status` (producto y variación; la variación puede traer `manage_stock:'parent'`); `importador.ts` `espejoStock()` → `ItemImportable.stockCanal/manejaStockCanal` (sin gestión de cantidad: `outofstock`→0, si no null), escritos en `ProductoCanal` (create y update) con `stockCanalEn`, y AMBOS entran al hash de sync (criterio 10: un cambio solo de stock en Woo refresca el espejo en el incremental). Corrida real onplaygames_cl: 467/469 con cantidad (174 en 0). `GET /stock/alertas` (encargado): `conteos` + listas `negativos`/`quiebres`/`bajos`/`web` (web = canal con `stockCanal ≤ 1`; `nivel` `reservado` si marca 0 y hay stock propio ≥1 —§6.9—, si no `ultimo`). `GET /stock/export.csv?ubicacionId` (BOM + `sku,nombre,ubicacion,cantidad,stockMinimo`, `Content-Disposition`). Web: V22 `pantallas/admin/Alertas.tsx` (`/admin/stock/alertas`, 4 secciones con Recontar → crea recuento de 1 producto en la ubicación de venta y abre el detalle / Ajustar / Ingresar), enlace «Alertas (N)» y «Exportar CSV» en V19 (descarga con `descargar()` de `api.ts`: fetch con Bearer + blob, porque el token vive en memoria y un `<a href>` no lo lleva), `Kardex` plegable en la ficha V5 (`GET /productos/:id/movimientos`, motivo en castellano, signo con color). Verificado por curl y en Chrome. **Fase 4 cerrada el 2026-09-02.** **E2 Fase 5 (devoluciones C8 y movimientos de caja C9) completada:** `apps/api/src/rutas/devoluciones.ts`: `POST /ventas/:id/devoluciones` (encargado; exige turno ABIERTO del que devuelve → 409 TURNO_NO_ABIERTO; la venta puede ser de un turno cerrado; `validarDevolucion` del dominio → 422 CANTIDAD_EXCEDE_VENTA/VENTA_ANULADA/…; `medio` efectivo|monedero (exige `clienteId` → MovimientoMonedero `devolucion` +)|otro (fuera del sistema, no toca arqueo); tx con orden Cliente → StockActual → Correlativo `devolucion_{año}` (folio `D-2026-00001` vía `formatearFolioDevolucion`); reposición `devolucion` + por línea con `reponeStock` y producto con control (dañado = sin movimiento); Auditoria `devolver`), `GET /ventas/:id/devoluciones` (con `yaDevueltas` por línea), `GET /devoluciones?desde&hasta&pagina`, `POST/GET /turnos/:id/movimientos-caja` (ingreso|retiro, monto > 0, nota obligatoria, turno abierto; Auditoria `editar` sobre turno_caja). `turnos.ts`: `extrasDelTurno()` alimenta `calcularArqueo(..., extras)` en cerrar y el resumen devuelve `devolucionesEfectivo/ingresosCaja/retirosCaja/montoEsperado` (el esperado en vivo lo calcula el servidor). Anular una venta con devoluciones → 409 VENTA_CON_DEVOLUCIONES. UI: V23 `components/DialogoDevolucion.tsx` (vendida/devuelta/a devolver por línea, casilla repone, medio, motivo, estimado; aviso si el turno de la venta cerró; éxito con folio e Imprimir), botón «Devolver…» siempre en V7 junto a «Anular…» (solo turno abierto); V24 `components/DialogoMovimientoCaja.tsx` desde el botón «Caja ±» del Mostrador (solo encargado+); V8 y `DialogoCierre` muestran las líneas nuevas del arqueo y usan `montoEsperado` del servidor. Verificado por curl (criterios 11 prorrateo 4600/3=1533, 12 devolución de venta de turno cerrado sale del turno abierto del encargado, 13 retiro exige nota y resta, 14 vendedor 403) y en Chrome. **Fase 5 cerrada el 2026-09-02. Fase 6 (cierre) hecha el 2026-09-02:** README con sección E2, spec 03 con estado y criterios marcados (17/18 ✅; criterio 1 verificado con 1.000 movimientos concurrentes en 3 s y `verificar` sin diferencias; criterio 6 con `idempotencyKey` repetida), `docs/09-guia-inventario-encargado.md` (guía operativa), bitácora R-013. **E2 queda a la espera del criterio 18 (dos recuentos reales que cuadren) y del despliegue**, que exige los 7 días de E1 en producción. Spec: `docs/03-SDD-etapa2-inventario.md` v1.0 (defaults, con D-E2-1 CAMBIADA por R-014 el 2026-09-02: **el stock nunca queda negativo** — `registrarMovimiento` lanza `422 STOCK_INSUFICIENTE` y la venta entera se aborta; el mostrador aplica el tope con `stockVenta` del caché y apaga el «+»; el delta de `catalogo-offline` incluye cambios de `StockActual`/`stockCanalEn` y el mostrador refresca el caché tras cada venta; **el SQL crudo del libro usa `UTC_TIMESTAMP(3)`, nunca `NOW(3)`** (MariaDB local vs Prisma UTC) — ; descuenta de la única ubicación `esVenta` = `mostrador`; `bodega` es la `publicable` para E3; umbral «último en la web» `stockCanal <= 1`; se construye en local ya). Modelo: `Ubicacion`, `MovimientoStock` (libro con signo, enum `MotivoStock` de 01 §6.2), `StockActual` (resumen materializado, SOLO se escribe desde `registrarMovimiento` con `FOR UPDATE`, orden de candados Cliente → StockActual → Correlativo), `Recuento`/`RecuentoLinea` (cerrar enciende `controlaStock`), `Devolucion`/`DevolucionLinea` (folio `D-año-#####`, cierra el hueco de E1 «turno cerrado → devolución»), `MovimientoCaja` (ingreso/retiro, entra al arqueo), `Producto.stockMinimo`, `ProductoCanal.stockCanal/stockCanalEn/manejaStockCanal` (espejo de solo lectura, R-011). E2 NO escribe en los canales. Fases: F1 cimientos → F2 libro+venta (test de concurrencia obligatorio) → F3 recuentos → F4 alertas+espejo → F5 devoluciones+caja → F6 cierre.

**R-010 (2026-09-03):** el patrón MTG de `skuMaestroDesdeExterno` acepta sufijo de un carácter en el número («240p», «116s», «259★») y The List (`PLST-SET-NUM` → `MTG-PLST-SET-NNN-…`). Script `npm run renumerar-ind -- [--aplicar]` renumera los `IND-` derivables con auditoría (dev: 235 hechas, queda `IND-000048` PPRO-2022-3). En producción correrlo una vez tras la importación inicial. **R-015 (2026-09-03):** `registrarErrorSync` en `importador.ts` no vuelve a insertar en `SyncLog` un error ya abierto con el mismo canal y `detalle` (completa e incremental); marcarlo resuelto lo deja reaparecer.

**Etapa 3 (sincronización bidireccional) EN CURSO desde 2026-09-03** — spec `docs/06-SDD-etapa3-sincronizacion.md` v1.1 (vinculante; estado en su cabecera; decisiones en `08` R-016/R-017). Fase 0 con defaults: se construye en local; el push real exige del dueño claves de escritura, `SYNC_SOLO_LECTURA=false`, respaldo restaurado y staging (`PATCH /canales/:id` responde `422 CANDADO_SOLO_LECTURA` mientras tanto). **F1 (cimientos) ✅:** migración `20260903201100_e3_sincronizacion` (`Canal.ingestaPedidos/pushPrecio/pushStock` + marcas `ultimaIngestaEn/ultimoPushPrecioEn/ultimoPushStockEn`; `ProductoCanal.precioPublicado/stockPublicado/publicadoEn/syncPrecio/syncStock/syncMensaje`; enums `EstadoSync/TipoDiscrepancia (+pedido_sin_stock al final)/EstadoDiscrepancia/TipoCorrida/EstadoCorrida`; modelos `Discrepancia` (una ABIERTA por sujeto y tipo vía `claveAbierta String? @unique` = `"pc:<id>|ped:<id>|lin:<id>:<tipo>"`, null al cerrar), `SyncCorrida`/`SyncCorridaItem`, `PedidoCanal` (`@@unique([canalId, externoId])`, `modificadoEnCanal`)/`PedidoCanalLinea` (`@@unique([pedidoId, externoItemId])`); back-relations Usuario/Producto/Cliente.pedidos); `packages/woo-client`: `solicitar` acepta cuerpo, `paginarPedidos(status, modificadosDesde)`, `listarReembolsos`, `listarProductosPorIds` (include ≤100), `obtenerProducto/Variacion`, `actualizarProducto/Variacion` (PUT acotado a `regular_price`/`stock_quantity`, test), tipos `PedidoWoo/ReembolsoWoo/CambiosProductoWoo`; dominio `packages/dominio/src/sync.ts` (`decidirPushStock` §4.1/§7.3/§7.4, `decidirPushPrecio` §4.3, `explicarDeriva`, `pedidoPagado/Anulado`, `devueltasPorLinea`, `montoDesdeWoo`; 16 tests, 91 en dominio); `apps/api/src/sync/corridas.ts` (`abrirCorrida` = `SELECT Canal FOR UPDATE` + check `en_curso` → 409 CORRIDA_EN_CURSO; `cerrarCorrida`; `abortarCorridasColgadas` en `index.ts` antes de `listen`; `clienteDelCanal`; `usuarioSistemaId` = semilla `sistema@onplay.cl` inactivo), `sync/discrepancias.ts` (`registrarDiscrepancia` upsert por clave → `vecesVista+1`; `cerrarDiscrepancia`; `TITULO_DISCREPANCIA`), `sync/completa.ts` (pedidos → precios → stock; precios/stock devuelven `omitida` hasta F3/F4), cron `SYNC_CRON_COMPLETA` (*/15, solo canales con algún interruptor) en `sync/cron.ts`; env §11 en `entorno.ts` (`syncUbicacionOnline`=bodega, `syncVentanaIngestaMin`, `syncLote`, `syncConcurrencia`, `alertaDiscrepancias`, `alertaCorreo`); rutas `rutas/canales.ts` (`GET /canales` encargado con últimas corridas; `PATCH /canales/:id` admin: `pushStock` sin `ingestaPedidos` → 422 INGESTA_REQUERIDA, encender ingesta sin marca fija `ultimaIngestaEn = ahora`, auditado). **F2 (E3c ingesta) ✅:** `apps/api/src/sync/pedidos.ts` `ingerirPedidos` (§8: consulta A `processing,completed` + B `cancelled,refunded` con `modified_after` desde `ultimaIngestaEn` y `dates_are_gmt`; simulación sin marca mira 24 h; real exige `ingestaPedidos` → 409 INGESTA_APAGADA; por pedido una tx: upsert por `(canalId, externoId)`, líneas upsert, mapeo por `variation_id/product_id` → `externoSku`, cliente vía `ClienteCanal`, `producto_sin_mapear` enlazada a la línea, descuento `venta_online` en `bodega` de `cantidad − cantidadDevuelta` con `permitirNegativo` (R-016: **única** excepción a R-014, solo motivo `venta_online`) y `pedido_sin_stock` si queda negativo; ya ingerido + cambió → `pedido_anulado` (pedido) o acotada a líneas con más devueltas; sin cambios → omitido; marca de agua avanza solo en corrida real terminada), `mapearLineaPedido` (descuenta lo que la ingesta no pudo, resuelve la discrepancia, audita `pedido_canal`); rutas `rutas/sincronizacion.ts`: `POST /sync/:canalId/pedidos|completa` (admin, dryRun default), `GET /sync/corridas[/:id]`, `GET /sync/pedidos?sinMapear`, `POST /sync/pedidos/:pedidoId/lineas/:lineaId/mapear`, `GET /sync/discrepancias[/:id]` (encargado, `abiertasPorTipo`, `titulos`), `POST /sync/discrepancias/:id/resolver` (`adoptar_canal` = ajuste con la diferencia en bodega + `stockPublicado`; `descartar`; `contactar_cliente`/`reembolsar`/`reponer {cantidad}` = compra para `pedido_sin_stock`; `imponer_maestro` admin → 501 hasta F4; nota obligatoria salvo descartar; 409 si cerrada; auditado). Verificado por curl contra onplaygames.cl real (criterios 1, 7, 10, 12, 13, §8.3, mapeo manual, vendedor 403). **F3 (E3a precio) y F4 (E3b stock + adopción) ✅:** motor `apps/api/src/sync/push.ts` (`publicarPrecios`, `publicarStock`, `adoptarStock`, `imponerMaestro`, `publicarProductoADemanda`): `vinculosElegibles` (publicado, externoId, producto activo; stock exige `controlaStock` — filtro duro §7.3, el resto se reporta como `noElegibles`), `leerCanal` (simples por `listarProductosPorIds`, variaciones por `listarVariaciones(padre)` — **`atributos.padreExternoId` es STRING**, se lee con `Number()`; lo que no vuelve = `producto_desaparecido` → `publicado=false` + discrepancia), decisión con el dominio, PUT con `enParalelo(4)`, `stockPublicado/precioPublicado` + `syncX='al_dia'` DESPUÉS del 200, fallo → `syncX='error'` + `syncMensaje` + ítem `fallar` sin abortar el lote; simulación no toca nada; `publicarStock` real exige corrida `pedidos` real terminada en `SYNC_VENTANA_INGESTA_MIN` (si no → corrida `abortada` con `omitida`); espejo `stockCanal` se refresca de paso; adoptar = `stockPublicado = S_canal` sin escribir en el canal + cierra `primera_publicacion` + Auditoria `canal`/`ajustar_stock`; marcas `ultimoPushPrecioEn/StockEn` solo en corrida real completa sin fallos. Rutas: `POST /sync/:canalId/precios|stock|adoptar?dryRun&productoIds=a,b` (admin), `POST /productos/:id/publicar` (encargado) y hook G9 en `PATCH /productos/:id` al cambiar `precioVenta` (fire-and-forget, solo canales con `pushPrecio`), `imponer_maestro` en resolver (admin; PUT real; con candado → 422 CANDADO_SOLO_LECTURA), `completa` = pedidos → precios → stock (stock omitido si la ingesta de esa corrida no corrió). Importador §5.3: `ContextoImportacion.pushPrecio/pushStock` (`interruptoresDelCanal`): con `pushPrecio` no escribe `precioVenta` y abre `precio_derivado`; con `pushStock` un producto no-publish abre `producto_desaparecido` en vez de despublicar. Enum `canal_sin_gestion` al final de `TipoDiscrepancia` (migración `20260903203328_e3_canal_sin_gestion`). Verificado por curl (criterios 1, 4, 5, 9, 10, 11, 15; G9; §5.3) con el candado puesto: las escrituras reales fallan con `SYNC_SOLO_LECTURA` como corresponde. **F5 (pantallas) y F6 ✅ (2026-09-03):** `apps/web/src/tiposSync.ts`; V12 `pantallas/admin/Sync.tsx` reescrita (estado del candado en vez de la insignia SOLO LECTURA; por canal: 3 interruptores con `PATCH /canales/:id`, marcas, última corrida; botones Simular corrida completa (principal) / Publicar (confirm) / Ingerir pedidos ahora / Simular-Adoptar stock / Simular solo stock; pull de E1 plegado en `<details>`; bitácoras conmutables Corridas (E3, `GET /sync/corridas`) y Catálogo (E1); `Plan` muestra ítems no omitidos con los tres números); V13 `Discrepancias.tsx` (`/admin/discrepancias`, encargado; grupos por tipo en `ORDEN_TIPOS`, tarjetas con maestro·canal·publicado, `vecesVista`, acciones por tipo (`accionesDe`) con consecuencia escrita (`consecuencia()`), diálogo con nota obligatoria salvo descartar, cantidad en reponer, `imponer_maestro` solo admin; enlace a V14 para `producto_sin_mapear`); V14 `PedidosOnline.tsx` (`/admin/pedidos?canalId&sinMapear=true`, tabla + fila expandible + `DialogoMapear` con `GET /productos/buscar` y `POST …/mapear`); rutas en `App.tsx` y menú en `BarraLateral.tsx` (⧈ Pedidos online, ⚠ Discrepancias, encargado). F6: `?soloErrores=true` en `POST /sync/:canalId/precios|stock` (G10, filtra `syncPrecio/syncStock='error'`), purga `SyncCorridaItem` > 90 días en `correrCompletas`, corte RS4 en `publicarStock` real (abiertas accionables > `ALERTA_DISCREPANCIAS` → `pushStock=false`, Auditoria `canal`, `mensaje` en la corrida y `advertencia` en la respuesta; sin correo por P6). **Pendiente:** revisión visual de V12/V13/V14 en Chrome (typecheck + `vite build` limpios). **E3 completa en local**; para producción: Fase 0 del dueño (claves de escritura, candado, respaldo, staging) y los plazos de 7 días entre subetapas (§9).

**Estado actual:** E1 completa; **Etapa 4 en curso** (spec `docs/07-SDD-etapa4-cliente-monedero.md`, vinculante). Defaults de Fase 0 aplicados: se construye en local ahora (los 7 días de E1 en producción son precondición para PONER E4 en producción, no para escribirla); campos de crédito C8 en el schema pero apagados y la Fase 5 (crédito+fusión) BLOQUEADA hasta aprobación explícita del dueño; producto semilla «Carga de saldo». **E4 Fase 1 (cimientos) completada:** migración `20260901191122_e4_cliente_monedero` — modelos `Cliente` (nombre único obligatorio M2, `rut @unique` normalizado, `nombreBusqueda` indexado para prefijo, `permiteCredito`/`limiteCredito` default false/0), `ClienteCanal` (`@@unique([canalId, externoUserId])` y `([clienteId, canalId])`, `desvinculadoEn` no borra M4), `MovimientoMonedero` (libro append-only, monto CON SIGNO, enum `MotivoMonedero`, `usuarioId` obligatorio M5), `Venta.clienteId` opcional + índice (clienteNombre SE MANTIENE), `monedero` AL FINAL de `MedioPago` (§5.4 rendimiento), back-relations en `Usuario`/`Canal`; NO se creó `Cliente.pedidos`/`PedidoCanal` (E3 no existe). Semilla idempotente `SRV-000001 · Carga de saldo` (tipo `servicio`, controlaStock false, precio 0 — el monto lo pone la línea) en `prisma/seed.ts`. Dominio E4 en `packages/dominio`: `rut.ts` (módulo 11 + normalización "12345678-9", DV K mayúscula), `monedero.ts` (`calcularSaldo` = SUM §6.1, `validarPagoMonedero` §6.2 → SALDO_INSUFICIENTE/{saldo,solicitado,falta} o LIMITE_CREDITO_EXCEDIDO, `validarMovimientoManual` §7.2 → motivo nunca consumo/devolucion, signos por motivo, nota obligatoria en ajuste/reverso_carga), `cliente.ts` (`nombreBusquedaCliente`, `detectarDuplicadosCliente` §6.6 rut/email=alta, teléfono=media, nombre=baja); 27 tests nuevos, 49 en dominio total. **E4 Fase 2 (clientes en el mostrador C1/C2/C3/C6, sin monedero) completada:** API en `apps/api/src/rutas/clientes.ts` (registrada en `servidor.ts`): `GET /clientes/buscar?q=` (vendedor, máx 10, activos, por nombreBusqueda prefijo/rut normalizado o contains/teléfono/email, con saldo calculado — criterio 17 «Ped» encuentra «Pedro» verificado), `GET /clientes` (encargado, filtros q/conSaldo/conCredito/activo, cursor), `GET /clientes/:id` (ficha con saldo SUM y canales), `GET /clientes/:id/compras` (vista REDUCIDA para vendedor sin folio/vendedor — criterio 18 verificado; encargado ve todo; `pedidosDisponibles:false` hasta E3), `POST /clientes` (rol VENDEDOR, única escritura de vendedor; RUT_INVALIDO con mensaje de la spec; duplicados §6.6: alta → 409 CLIENTE_DUPLICADO, media/baja → crea con `posiblesDuplicados`; auditado), `PATCH /clientes/:id` (encargado; tocar crédito exige `nota` → 422 NOTA_REQUERIDA; auditado con anterior/nuevo). HC2 CERRADO: `POST /ventas` acepta `clienteId` opcional (valida existencia+activo → 422 CLIENTE_NO_ENCONTRADO; `clienteNombre` libre se mantiene, M3); `monedero` sigue rechazado como medio hasta F3. UI: `components/SelectorCliente.tsx` (V15 sin monedero: buscador con debounce 250ms en el cobro, resultados con nombre/RUT/saldo≠0 — negativo `text-peligro` «debe $X» —, elegir asocia, «Crear cliente» = alta inline de 2 campos nombre+teléfono sin salir del cobro M2, texto libre → clienteNombre M3, offline no busca pero el texto queda), integrado en `DialogoCobro` con estado en `Mostrador.tsx` (viaja en `construirCuerpo` → cola offline incluida; se limpia en ventaLista/F8); `pantallas/Cliente.tsx` (V16 parcial: ruta `/clientes/:id`, saldo en text-total tinta/peligro, secciones Compras con insignia de origen y Cuentas vinculadas; Movimientos y «Cargar saldo» llegan en F3); tipos E4 en `tipos.ts` (`ClienteResumen`, `FichaCliente`, `CompraCliente`, `ETIQUETA_ORIGEN`), `CuerpoVenta` de `cola.ts` con `clienteId`/`clienteNombre`. **E4 Fase 3 (monedero C4/C5) completada:** `POST /ventas` acepta `medio:"monedero"` (§6.2, dentro de la MISMA transacción: `SELECT … FOR UPDATE` sobre la fila Cliente PRIMERO y Correlativo SIEMPRE al final — orden fijo anti-interbloqueo —, saldo por SUM, topes vía `validarPagoMonedero` → 422 SALDO_INSUFICIENTE/{saldo,solicitado,falta} o LIMITE_CREDITO_EXCEDIDO lanzados como `ErrorMonedero` desde la tx, movimiento `consumo` negativo con referencia a la venta); carga §6.3 = venta de `SRV-000001` (constante `SKU_CARGA`; genera movimiento `carga` positivo + Auditoria; sin clienteId → 422 CLIENTE_REQUERIDO; carga nunca se paga con monedero → 422 MEDIO_PAGO_INVALIDO); anulación §6.4/HC3: por cada pago monedero un movimiento POSITIVO `devolucion` (el consumo original NO se toca, M4) y si la venta tenía líneas SRV-000001 un movimiento NEGATIVO `reverso_carga` auditado, ambos con nota `Anulación de <folio>: <motivo>`. `GET /clientes/:id/movimientos` (saldo corriente `saldoDespues`; vendedor SIN usuario/referencia — criterio 18) y `POST /clientes/:id/monedero` (encargado; `validarMovimientoManual` → MOTIVO_NO_MANUAL/SIGNO_INVALIDO/NOTA_REQUERIDA; auditado con saldo anterior/nuevo — criterio 11). `MedioPago` del dominio y de la web ganó `'monedero'`. UI: botón Monedero en `DialogoCobro` (7º medio, teclas 1–7; deshabilitado sin cliente o sin disponible; etiqueta `Monedero · $X`; al elegirlo precarga `min(falta, disponible)`; disponible = saldo + límite si permiteCredito − ya asignado a monedero; si se quita el cliente a mitad del cobro se descartan los pagos monedero), V16 completo en `pantallas/Cliente.tsx` (sección Movimientos con motivo en castellano/monto con signo/saldo corriente, botón «Cargar saldo» solo encargado) y V17 `DialogoMovimiento` (motivos Premio/Ajuste/Reverso — SIN carga, esa va por venta —, signo automático con selector Sumar/Restar en ajuste, nota obligatoria en ajuste/reverso, vista previa «El saldo pasará de $X a $Y»). Verificado por curl (criterios 5,6,7,9,10,11,12,16,18) y **test de concurrencia criterio 8 ok**: dos POST /ventas simultáneos por el saldo completo → exactamente una 201 y una 422, saldo final 0. **E4 Fase 4 (vinculación con los canales C7) completada:** `packages/woo-client` ganó `UsuarioWoo` y `paginarClientes()` — lee **`wc/v3/customers` (NUNCA `wp/v2/users`)**: las mismas claves ck_/cs_ de solo lectura autentican y el `id` ES el `externoUserId` de `ClienteCanal`. Importador `apps/api/src/sync/clientes.ts` (`importarClientesCanal`): matching por correo en memoria (volumen chico), contadores totalCanal/yaVinculados/desvinculados/sinEmail, listas `vinculos` (coincidencias), `sinCoincidencia` (SIEMPRE propuestas: crear es decisión humana §7.3, criterio 19) y `conflictos` (cliente con otra cuenta activa en el canal); cuentas desvinculadas a mano NO se re-vinculan (se respeta la decisión); corrida real (`dryRun=false`) solo VINCULA coincidencias con `clienteCanal.upsert` por la unique `[clienteId, canalId]` (reutiliza fila desvinculada) + Auditoria por vínculo; cada corrida deja el resumen JSON en SyncLog (`operacion:'clientes'`) que alimenta los candidatos. Rutas: `POST /sync/:canalId/clientes` (admin, dryRun por defecto S1), `GET /clientes/candidatos` (encargado; última corrida por canal filtrando lo ya vinculado — la ruta estática gana a `/clientes/:id` en el radix tree de Fastify), `POST /clientes/:id/vincular` (encargado; 409 CUENTA_YA_VINCULADA si la cuenta es de otro cliente aunque esté desvinculada — la unique `[canalId, externoUserId]` lo impide — y 409 CLIENTE_YA_VINCULADO si tiene otra cuenta activa en el canal; auditado), `POST /clientes/:id/desvincular` (encargado; marca `desvinculadoEn`, NO borra — criterio 20 verificado; auditado). `GET /clientes` ganó `conDeuda` (SUM<0), `orden=saldo|ultimaCompra` (agregado completo en memoria, sin cursor) y columna `ultimaCompra` (groupBy Venta completada `_max creadoEn`). UI: V18 `pantallas/admin/Clientes.tsx` (`/admin/clientes`, barra lateral icono ◉): tabla nombre/contacto/saldo/crédito/última compra, filtros Todos/Con saldo/Con deuda/Con crédito/⚠ Duplicados (duplicados = rut/email/teléfono repetido entre filas cargadas, en memoria), orden por saldo/última compra, y sección «Candidatos de las tiendas web» por canal con botón Vincular por fila, conflictos en Banner y las sin-coincidencia plegadas con la nota de que crear es decisión humana; botón «Buscar clientes» (dryRun) en las tarjetas de canal de `Sync.tsx`; acción Desvincular (confirm + POST) en Cuentas vinculadas de la ficha, solo encargado. Verificado por curl contra onplaygames_cl real: dryRun 165 cuentas, matching por correo tras asignar un email del canal a un cliente de prueba, corrida real creó el vínculo, la ficha lo muestra, desvincular dejó la fila con `desvinculadoEn` (misma fila reutilizada al re-vincular), la importación posterior contó `desvinculados:1` sin re-vincular, y el 409 a otro cliente respondió con el detalle M4. 63 tests verdes, build limpio. Siguiente: **Fase 5 (crédito+fusión C8/C9) BLOQUEADA hasta aprobación explícita del dueño**; Fase 6 (desinstalar plugins de monedero viejos) depende del despliegue a producción.

**Estado E1:** Fases 0–8 de E1 completadas (queda el despliegue real y los criterios §10 que dependen del usuario). Fase 8 (cierre de E1, §11+§13): **§11 env** — `SYNC_HABILITADO` (default true; `false` apaga el cron con log) y `SYNC_CRON` (default `*/30 * * * *`) en `entorno.ts`, respetados por `cron.ts`. **Producción same-origin (P6/H7)** — con `NODE_ENV=production` el MISMO proceso API sirve `apps/web/dist` vía `@fastify/static` con fallback SPA (`setNotFoundHandler`: GET no-`/api/` → index.html; `/api/*` no hallado → 404 `{error:'NO_ENCONTRADO'}`); la web usa rutas relativas `/api/v1` sin ninguna `VITE_*`. **Runtime de producción: tsx, NO node puro** — `packages/dominio` y `packages/woo-client` tienen `main: src/index.ts` (fuente única sin build propio), así que `node dist/` falla con ERR_UNKNOWN_FILE_EXTENSION; `apps/api` tiene `"start": "tsx src/index.ts"` y pm2 se arranca desde `apps/api/` con `pm2 start npm --name onplay-core -- run start`; `npm run build` queda como puerta de typecheck + genera `apps/web/dist`. README.md de la raíz creado según §13 (instalación, variables, despliegue). Smoke de producción local verificado (estáticos 200, fallback SPA 200, 404 JSON de API, cron deshabilitable). Fase 7 (endurecimiento F10/F11/§6.5): **F10 cola offline** — IndexedDB `onplay-mostrador` v2 con store `ventasPendientes` (keyPath = `idempotencyKey` PERSISTIDA junto a la venta, nunca regenerada), módulo `apps/web/src/cola.ts` (encolar → flush al reconectar vía evento `online`, arrancado en `Armazon` de App.tsx; ErrorApi ≠401 marca fallida y sigue, fallo de red aborta la corrida), `DialogoCobro` offline encola sin diálogo extra (botón «CONFIRMAR VENTA · sin conexión», éxito «Pendiente de enviar» con folio null; si el fetch lanza TypeError a mitad de cobro también encola — la clave persistida evita duplicar), `IndicadorConexion` con los 4 estados de 05-SDD §8.1, banners en Mostrador (ok 4 s «Se enviaron N…» / peligro persistente con «Ver detalle»). **F11 fusión** — `POST /productos/:id/fusionar` (`:id` = sobreviviente, body `{productoAbsorbidoId}`, encargado): transacción que reasigna `ProductoCanal`, conflicto mismo canal → gana `sincronizadoEn` más reciente (el que pierde se elimina y queda en Auditoria), absorbido `activo=false` (P9); se audita como DOS registros `accion='editar'` (el enum AccionAuditoria NO tiene `fusionar` — decisión: no divergir del schema §4.1); botones de `Duplicados.tsx` habilitados con `window.confirm`. **§6.5 incremental** — `paginarProductosModificados` en woo-client (`modified_after` + `dates_are_gmt=true` OBLIGATORIO por TZ Chile, `status=any` para despublicar borradores), `sincronizarIncremental` en `importador.ts` (marca de agua = `detalle.hasta` del último SyncLog incremental ok, fallback max `sincronizadoEn`; solo actualiza vía `procesarItem` que omite por hash; producto no-publish → `publicado=false`, nunca borra), cron node-cron `*/30 * * * *` solo en `index.ts` (no en tests) con lock booleano, endpoint manual `POST /sync/:canalId/incremental` (admin). Verificado: 2ª corrida parte del `hasta` de la 1ª; 3 ventas con claves repetidas 2 veces → exactamente 3 folios, reintentos devuelven la original con `repetida:true`. Fase 6 (backoffice F7, `apps/web/src/pantallas/admin/`): rutas `/admin/*` protegidas con `RequiereRol` (cliente) + roles server-side; barra lateral filtrada por rol (`rolAlcanza` en `tipos.ts`) — un vendedor nunca ve `/admin`. Pantallas: V5 Productos (cursor 50/pág, sin contador total, filtros q/tipo/categoría/⚠duplicados/activos, expandir fila → canales vía GET detalle, cambio de precio con confirmación viejo→nuevo auditada), V6 Alta de snack (4 campos, `tipo` derivado de la categoría raíz vía `TIPO_POR_RAIZ` y SIEMPRE explícito, Enter = «Guardar y otro» que conserva la categoría y refoca nombre, confirmación 2 s con el SKU, aviso de duplicado en vivo no bloqueante desde el caché local), V7 admin Ventas (filtro fecha+estado, anular con motivo obligatorio y aviso «no devuelve dinero ni repone stock»; turno cerrado → botón deshabilitado con mensaje de devolución E2), V8 Turnos (diferencia $0 ok / falta·sobra alerta; turno abierto muestra Esperado en vivo = apertura + efectivo del resumen), V9 Duplicados (pares lado a lado, botones de fusión deshabilitados con «La fusión llega en la Fase 7» — el endpoint fusionar es F11), V10 Sync (solo admin, insignia permanente SOLO LECTURA, Simular primario / Importar secundario con confirm, bitácora con «Marcar resuelto»), Auditoría (H2 resuelto: GET /auditoria existe desde Fase 3; cambio de precio legible, resto JSON plegado). `/admin/usuarios` NO existe (H1, sin endpoints). Atajo F4 = ir al backoffice (encargado+). **Ojo Tailwind:** `tailwind.config.js` usa escala de spacing CERRADA (theme reemplaza, no extiende): clases como `w-40`/`h-9`/`pb-24` NO generan CSS — usar valores arbitrarios (`w-[160px]`) o los tokens nombrados (`tactil`, `boton`, `fila`, `item-barra`). Fase 5 (PWA mostrador, `apps/web`): React 19 + Vite 5 + Tailwind 3.4 sin librerías de componentes, diseño vinculante `docs/05-SDD-diseno-interfaz-etapa1_1.md` («Cristal OnPlay», tokens CSS por `data-tema`, solo teclas de función como atajos). Pantallas V0 (entrar), V1 (apertura no cerrable, 409 carga el turno), V2 (buscador con foco permanente + caché IndexedDB local-first con delta `?desde` cada 30 min + accesos rápidos + panel de venta), V3 (cobro por teclado: 1–6 medio, Enter agrega por lo que falta, segundo Enter confirma; `idempotencyKey` ULID estable ante reintentos), V4 (cierre con desglose del arqueo, nota obligatoria si diferencia≠0, `window.print()` en B/N vía `.solo-imprimir`), V7 lite (`/mis-ventas`). Sesión H7: refresh token en cookie httpOnly SameSite=Strict (`/auth/refresh` acepta cookie, `/auth/salir` la limpia); dev same-origin vía proxy Vite `:5183 → :3010`. **Ojo:** vite fijado en `^5.4` para no duplicar la copia hoisted de vitest (dos vites rompen el typecheck del config); la cola offline de ventas y la anulación en UI son Fase 7. Fase 4 (venta y caja F5/F6): reglas puras en `packages/dominio/src/venta.ts` (`validarYCalcularVenta` con validaciones 3–9 de §5.4, `calcularArqueo` §5.3) y rutas `/api/v1/turnos*` (abrir con 409 `TURNO_YA_ABIERTO`, actual, cerrar con arqueo que incluye `montoApertura` y `422 NOTA_REQUERIDA` server-side, resumen por medio de pago, historial encargado) y `/api/v1/ventas*` (POST con turno abierto, idempotencia → original con 200 incluso en carrera P2002, descripción y precio congelados con `advertencias` + Auditoria, folio `V-año-#####` vía `SELECT ... FOR UPDATE` en transacción única — folios concurrentes verificados consecutivos —, GET restringido: vendedor solo su turno abierto sino 403, anular solo encargado con motivo y turno abierto sino 409 `TURNO_CERRADO`). Verificable cumplido: pago mixto, arqueo cuadra con apertura, anuladas fuera del arqueo, idempotencyKey no duplica. Fase 3 (catálogo F3/F4): rutas `/api/v1/productos*` (listado con filtros y cursor, `buscar` con fulltext + fallback contains y p95 real <50ms, `catalogo-offline` con gzip y delta por `?desde`, detalle con canales, alta con SKU correlativo automático, PATCH con auditoría de precio), `/categorias` (árbol) y `/auditoria`. gzip vía `@fastify/compress`. Fase 2 (importador F1): `packages/woo-client` (candado `SYNC_SOLO_LECTURA`), `packages/dominio` (SKU §6.4, duplicados §6.6), `apps/api/src/sync/` (mapeo §6.3 por canal + importador §6.2) y rutas `/api/v1/sync/*` (importar con dryRun por defecto, logs, estado). **onplaygames.cl importado**: 469 productos (478 ítems, variables expandidos por variación); errores conocidos en `SyncLog`: 2 sin precio y 9 SKUs duplicados en el origen (variaciones que comparten SKU con sufijo `-1` — se rechazan, revisión manual). D1 resuelta: hosting con Node. Línea base de onplay.cl actualizada a ~2.880 productos. **Pendiente (dependen del usuario, criterios §10):** claves `ck_`/`cs_` de solo lectura de onplay.cl e importación de ese canal; resolver los SyncLog abiertos hasta 0 (criterio 2); carga real de snacks con el encargado (criterio 4); prueba end-to-end de cambio de precio en Woo <35 min (criterio 7, el mecanismo ya está verificado); despliegue real a Hostinger y 7 días corridos en producción (criterio 11).

## Comandos

- **Instalar:** `npm install` (raíz; workspaces npm)
- **Dev API:** `npm run dev` (raíz) — levanta Fastify en `:3010` con tsx watch
- **Dev web:** `npm run dev -w @onplay/web` — Vite en `:5183` con proxy `/api` → `:3010` (mismo origen para la cookie H7)
- **Build web:** `npm run build -w @onplay/web` (tsc --noEmit + vite build)
- **Build completo:** `npm run build` (raíz) — typecheck de la API + build de la web; es la puerta previa al despliegue
- **Producción:** `npm run start -w @onplay/api` (tsx; con `NODE_ENV=production` sirve también `apps/web/dist`). Despliegue: `git pull && npm ci && npm run build`, `npx prisma migrate deploy` desde `apps/api/`, `pm2 reload onplay-core` (primera vez desde `apps/api/`: `pm2 start npm --name onplay-core -- run start`)
- **Tests:** `npm test` (raíz, todos los workspaces) o `npm test -w @onplay/dominio` para uno
- **Migraciones:** desde `apps/api/`: `npx prisma migrate dev` / `npx prisma migrate deploy` / `npx prisma validate` (el schema vive en `prisma/` de la raíz; `apps/api/package.json` lo apunta vía el campo `prisma.schema`)
- **Semillas:** `npm run seed` (idempotente, upserts)
- **Admin inicial:** `npm run crear-admin -- <email> <nombre> [password]`
- **Typecheck:** `npx tsc -p apps/api/tsconfig.json --noEmit`
- **MySQL local (dev):** MariaDB de XAMPP — arrancar con `C:\xampp\mysql\bin\mysqld.exe`; base `onplay_core`, usuario `root` sin clave. `DATABASE_URL` en `apps/api/.env` (no versionado)
- **Prisma está fijado en 5.x** a propósito: el esquema de la spec usa `previewFeatures = ["fullTextIndex", "fullTextSearch"]`, que en Prisma 6+ cambian de estado. No actualizar a Prisma 6+ sin ajustar el esquema y verificar contra la spec.

## Stack y estructura prevista (SDD §5)

- **Backend:** Node.js 20 + TypeScript estricto + Fastify. **Base de datos:** MySQL 8 con Prisma (migraciones versionadas). **Frontend:** React 19 + Vite + Tailwind 3.4, un solo bundle PWA (Mostrador + Backoffice) con IndexedDB para caché offline.
- Monorepo con **npm workspaces**, sin Turborepo/Nx: `apps/api`, `apps/web`, `packages/dominio` (tipos y reglas de negocio), `packages/woo-client` (cliente tipado de WooCommerce REST v3), `prisma/`, `docs/`.
- **Despliegue:** `git pull` + `npm ci` + `prisma migrate deploy` + `pm2 reload`. Sin CI/CD por ahora.

## Principios vinculantes (SDD §3)

Existen para impedir que el proyecto muera por alcance, como cuatro intentos anteriores. **No son negociables:**

- **P1** — Cada etapa entra en producción y es útil sola. Funcionalidad no listada en la spec de la etapa actual se rechaza y se agenda (Riesgo R4).
- **P2** — Espejo antes que control: primero se *lee* de WooCommerce; solo se *escribe* (E3) cuando la lectura es confiable.
- **P3** — Ningún SKU ya publicado se renumera. La correspondencia vive en la tabla `producto_canal` (`externo_id`, `externo_sku`), no en convenciones de nombres.
- **P4** — El stock es opcional por producto (`controlaStock`, default `false`).
- **P5** — Stock, caja y saldo de cliente son libros de movimientos **append-only**; el saldo es siempre `SUM()`, nunca un campo que se sobrescribe.
- **P6** — Un proceso Node, una base MySQL. Sin microservicios, colas, event bus ni herramientas de build extra hasta que un problema medido lo exija.
- **P7** — Español en el dominio (`producto`, `venta`, `turnoCaja`), inglés en la infraestructura (`Repository`, `Service`, `Controller`).
- **P8** — Offline-tolerante en mostrador: la PWA vende con catálogo cacheado y encola las ventas.
- **P9** — Nada se borra: anulaciones y correcciones son registros nuevos que compensan.

## Reglas de seguridad S1–S4 (SDD §8.3 — de defectos reales del Binder OP)

- **S1** — Toda escritura a un canal externo tiene modo `dryRun`, que es el **valor por defecto**. El dryRun no tiene efectos colaterales (tampoco crea categorías).
- **S2** — El push de stock exige verificación previa: leer el stock del canal y compararlo con `producto_canal.stockPublicado` antes de escribir. Si no coincide, **no se escribe**: se reporta como discrepancia. (La API `wc/v3` solo acepta `stock_quantity` absoluto.)
- **S3** — Ningún secreto llega al navegador. Credenciales de WooCommerce (`ck_`/`cs_`) solo en variables de entorno del servidor; auth por JWT + cookie. Nunca `VITE_*` con credenciales.
- **S4** — Toda escritura de datos es transaccional (MySQL). Nunca persistir estado en JSON a disco.
- **Tests desde el primer commit** para reglas de negocio: cálculo de total, arqueo de caja, generación de SKU, validación de pagos.

## Convenciones de código y datos

- **Prisma/MySQL en `camelCase`**, sin `@map` (el SDD documenta en `snake_case` solo por legibilidad: `precio_venta` ↔ `precioVenta`).
- **IDs:** `cuid` de Prisma en todas las entidades. Excepción: `idempotencyKey` de `venta` es un ULID generado por el cliente.
- **Precios en CLP como `int`**, sin decimales.
- `producto.juego` es **string libre, no enum** (juegos nuevos aparecen varias veces al año). `producto.tipo` sí es enum. Atributos específicos de tipo van en `atributos` JSON, no en columnas.
- **SKU maestro:** `{PREFIJO_TIPO}-{IDENTIFICADOR}` (`MTG-`, `OPT-`, `SLD-`, `ACC-`, `SNK-`, `JDM-`, `EVT-`). Es interno; **no se publica** en E1 y nunca reescribe un SKU existente en WooCommerce.
- **Folios** (`V-2026-00001`) se asignan con `SELECT ... FOR UPDATE` sobre la tabla `correlativo`, dentro de la misma transacción de la venta.
- Sincronización: lotes de 50, upsert por `externo_id` (llave secundaria `externo_sku`), un error por ítem nunca aborta el lote, todo se registra en `sync_log`. `auditoria` (acciones humanas) y `sync_log` (acciones de máquina) son tablas **distintas** — no mezclarlas.
- Ninguna venta se registra sin `turno_caja` abierto. Una venta de un turno cerrado no se anula (se resuelve con devolución en E2). El arqueo incluye `monto_apertura` y excluye ventas anuladas.
- Entidades por etapa (SDD §6.2): ninguna tabla se crea antes de la etapa que la necesita (`ubicacion`/`movimiento_stock` en E2; `cliente`/`movimiento_monedero` en E4; `costo_referencia` por migración en E6).

## Roadmap y dependencias (SDD §9)

E1 (mostrador: precio + venta + caja) → E2 (inventario) → E3 (sync bidireccional) y E6 (compras/márgenes); E4 (cliente/monedero, depende solo de E1) → E5 (eventos). En E3 no se avanza de subetapa (precio → stock → pedidos) hasta que la anterior corra 7 días sin discrepancias. Staging obligatorio desde E3.

## Sistemas existentes que NO se tocan

- **Binder OP** (Node + Puppeteer) y **plugin OnPlay Binder** (CSV + Scryfall) siguen siendo los cargadores de catálogo de cartas hacia onplay.cl; `onplay-core` los consume vía el pull de E1.
- El checkout de los sitios (Transbank, Mercado Pago, carrito Woo) sigue tal cual.
- Fuera de alcance: facturación electrónica SII, fusión de canales, grading/variantes foil en E1–E3.
